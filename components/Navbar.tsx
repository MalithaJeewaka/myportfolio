"use client";
import React, { useRef } from "react";
import gsap from "gsap";

const Navbar = () => {
  return (
    <div className="h-[5vh] w-full flex items-center absolute top-0 left-0 bg-white z-50"></div>
  );
};

export default Navbar;
